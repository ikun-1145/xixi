name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 0 */5 * *"   # 每5天执行一次
  workflow_dispatch:        # 允许手动触发

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase public table
        run: |
          python3 << 'EOF'
import requests

SUPABASE_URL = "https://klyrasrqgxijwrxuoevj.supabase.co"
SUPABASE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtseXJhc3JxZ3hpandyeHVvZXZqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTI4ODUyMzcsImV4cCI6MjA2ODQ2MTIzN30.qjeTrLp_QquSwvF09HrrQd-stPtgu6H51-Zdb4JUeSM"
TABLE_NAME = "public"

headers = {
    "apikey": SUPABASE_KEY,
    "Authorization": f"Bearer {SUPABASE_KEY}"
}
url = f"{SUPABASE_URL}/rest/v1/{TABLE_NAME}?limit=1"
print("Pinging:", url)
r = requests.get(url, headers=headers)
print("Status:", r.status_code, r.text[:200])
EOF
